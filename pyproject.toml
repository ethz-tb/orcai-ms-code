[project]
name = "orcai-ms-code"
version = "0.1.0"
description = "Code used in orcAI manuscript"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipython>=9.3.0",
    "orcai",
    "pydot>=4.0.1",
    "ruff>=0.12.1",
]

[tool.uv.sources]
orcai = { git = "https://github.com/ethz-tb/orcAI.git" }
